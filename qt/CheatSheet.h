#pragma once
#include <QString>

// Inline HTML for the built-in Help -> Command chart dialog. Content is
// based on the Ravenspiral GoatTracker Command Chart (v2.67 era) with
// the values verified against this repo's gpattern.c / gsong.c / gsid.c
// and extended with the Qt frontend's keyboard shortcuts.
QString cheatSheetHtml();
